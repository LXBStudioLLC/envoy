# Envoy — README assets

Images referenced by the top-level [`README.md`](../README.md). The PNGs here are **auto-generated placeholders** (the app's dark/cyan theme) so the README renders cleanly before real captures exist. **Replace them with real screenshots at the same paths** and the README picks them up automatically — no markdown changes needed.

| File | Used for | Suggested size |
|---|---|---|
| `banner.png` | Hero wordmark at the top of the README | ~1280×360 |
| `ghost-score.png` | Ghost Risk panel — risk band + confidence + evidence lines (the most important shot) | ~1640×920 (renders at 820px) |
| `find-jobs.png` | Find Jobs results list with inline risk bands | ~1640×920 |
| `apply-copilot.png` | Apply view paused at the Confirm / Cancel submit gate | ~1640×920 |

**Tips:** keep a consistent app theme, keep personal data out of frame (use a sample resume), and compress PNGs before committing. A short GIF of *drop resume → tailored → review-before-submit* works well in place of a static shot.

**Also recommended** (owner action — GitHub → Settings → General → Social preview): a 1280×640 `social-preview.png` for the link card shown when the repo is shared on X / LinkedIn / Slack / Show HN.
